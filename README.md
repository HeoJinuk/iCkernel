# Simple C Kernel for Jupyter

Windows 환경의 Jupyter Notebook에서 `scanf` 입력을 완벽하게 지원하는 C언어 커널입니다.

## 설치 방법
1. **GCC 설치:** MinGW 등을 설치하고 `gcc` 명령어가 되는지 확인하세요.
2. 이 폴더에서 아래 명령어를 실행하세요.
   ```bash
   python install.py